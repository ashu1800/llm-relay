# ===== 密钥与运行时数据（绝不入库）=====
.env
.env.local
deploy/.env
deploy/data/

# ===== 数据库与本地上传 =====
*.sqlite
*.db

# ===== Go =====
/backend/llm-relay
/backend/server
# 宿主侧更新器的构建产物。install.sh 把它装到 $INSTALL_DIR，
# 而本地直接 go build ./cmd/updater 会落在包目录里 —— 9MB 的二进制
# 不该跟着源码走。
/backend/updater
/backend/updater.exe
/backend/llm-relay-updater
/out/
*.test
*.out

# ===== 前端 =====
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# ===== 嵌入式前端产物：由 Dockerfile / npm run build 生成 =====
# 保留 .gitkeep 占位，否则 //go:embed all:dist 在全新克隆时会编译失败
backend/internal/web/dist/*
!backend/internal/web/dist/.gitkeep

# ===== 浏览器调试档案（UI 逆向时产生）=====
.chrome-profile/
.chrome-cdp/

# ===== 参考实现（只读对照，不入库）=====
# 实现版本更新功能时克隆 sub2api 到本地做对照。它是**别人**的完整源码树
# （几十 MB、含自己的 .git），留在工作区里会让任何全仓搜索都返回一半
# 无关结果，而它对使用者的价值是零。
.tmp-sub2api/

# ===== 日志与临时文件 =====
*.log
tmp/
.cache/

# ===== 编辑器与系统 =====
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
.shots/

# ===== 内部审查文档（刻意不入库）=====
# 代码审查报告逐条记录了未修复项、攻击面、以及安全加固的具体边界
# （SSRF 防护范围、错误脱敏边界、密钥轮换方式、privoxy 权限要求等）。
# 对外部读者等于一份攻击面地图，而它对使用者的价值远低于这个代价。
# 2026-09-23：本地那三份（首轮 / 第二轮 / 第三轮）已按站主要求清理，
# 这条规则留着，是为了下次再生成报告时不会误提交。
#
# 用通配而不是写死「代码审查报告.md」这一个名字：报告是按轮次出的
# （-第二轮、-第三轮…），逐份往这里加行的结果就是漏加 —— 第二轮那份
# 就是这么进了库、还被推到了公开的 origin。一个通配符把整族文件永久盖住。
代码审查报告*.md
