# 换行符：显式声明，不依赖每个人的 core.autocrlf。
#
# 为什么必须写这个文件：本机 core.autocrlf=true 且仓库里没有 .gitattributes，
# 于是全新 clone 时 git 会把文本文件一律检成 CRLF —— 对 shell 脚本是致命的：
#
#     $ bash -n scripts/verify-all.sh
#     scripts/verify-all.sh: line 12: syntax error near unexpected token `$'\r''
#
# 脚本本身的逻辑没问题，纯粹是行尾被改了。这类故障很费时间：
# 报错指向的行号是假的，而且「本地能跑、新机器上炸」。
#
# 仓库里现有的 .sh 在 index 里存的确实是 LF（用 git cat-file 逐字节确认过），
# 所以问题不在已提交的内容，而在检出行为 —— 用 eol=lf 把它钉死。
*.sh text eol=lf

# 容器与部署脚本同样是 Linux 侧执行，按 LF 处理
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
# systemd 单元同样是 Linux 侧的文件，而且 CRLF 对它是**硬故障**：
# systemd 会拒绝解析带 \r 的行（报 "Invalid argument" 之类），
# 于是 llm-relay-updater.service 装不上、一键更新静默失效。
# 它无扩展名，不声明就会走 core.autocrlf。
*.service text eol=lf

# 无扩展名但这几个是文本：不声明的话 autocrlf=true 会把它们在检出时
# 转成 CRLF（git add 时会警告「LF will be replaced by CRLF」）。
go.mod text eol=lf
go.sum text eol=lf
.dockerignore text eol=lf
.gitattributes text eol=lf
.gitignore text eol=lf
# deploy/.env.example 同理（.env 本体不入库，不需要声明）
.env.example text eol=lf

# Makefile 必须钉 LF。它同样是「无扩展名 + 由 Linux 侧工具读」的文件，
# 而 CRLF 对 make 的破坏是**静默的**：制表符后的 ^M 会被当成命令的一部分，
# 更隐蔽的是 $(shell ...) 的返回值会带上 \r ——
#   VERSION ?= $(shell sh scripts/resolve-version.sh)
# 检出成 CRLF 后 VERSION 会变成 "0.1.0\r"，拼进 -ldflags 就是一个带回车
# 的版本号，界面上看不出来、下载 URL 却会 404。
# 已实测：CRLF 的 Makefile 里 $(shell echo x) 求值结果是空而不是 x。
Makefile text eol=lf

# 版本文件同理：后端 //go:embed 它、构建脚本 tr -d '\r\n' 读它，
# 带 \r 的版本号会一路带进二进制。
backend/internal/version/VERSION text eol=lf

# 其余文本文件统一走 LF。
#
# 前端源码与 markdown 用 LF 不影响 Windows 上的编辑器/构建
# （Go 与 vite 都接受 LF，且 vite 产物本来就是 LF）。
*.go text eol=lf
*.ts text eol=lf
*.vue text eol=lf
*.css text eol=lf
*.html text eol=lf
*.md text eol=lf
*.json text eol=lf
*.mjs text eol=lf
# PowerShell 脚本：Windows 侧执行，CRLF 本身无害，但这里仍然钉 LF ——
# 理由是**一致性**而不是兼容性：不声明的话它们走 core.autocrlf，
# 每台机器 checkout 出来的行尾可能不同，diff 里会混入看不见的换行改动。
# （scripts/wsl-bash.psm1 是给 WSL 里的 bash 调用的工具，保持与其它
#   仓库文本同一种行尾，省得日后有人拿它当 .sh 用而踩 CRLF 的坑。）
*.ps1 text eol=lf
*.psm1 text eol=lf

# 二进制：明确标记，免得 git 去猜（也不会被 autocrlf 碰）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.gz binary
*.sqlite binary
*.exe binary

# 字体是二进制：不写这行会被 text=auto 当文本处理（检出时改换行、比对失真）
*.ttf binary
*.otf binary
