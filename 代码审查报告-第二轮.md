# llm-relay 第二轮代码审查报告（前端 UX / 后端功能与性能）

**审查日期**：2026-09-17
**审查方式**：三路并行独立审查（前端全部 / 后端 relay+convert / 后端 api+store+model+pricing+config）+ 主持者独立复查交叉验证。**所有中档以上发现均经主持者对照源码逐行核实**，无一虚报；低档发现由子代理给出代码依据。
**与第一轮报告的关系**：第一轮（代码审查报告.md）的 S/M/N 系列已全部修复或判定非缺陷；本轮不重复其"尚未处理"清单（L11 重复样板、L12/L13 工具链、L1 死代码等）。

---

## 〇、总览与最优先修复项

| # | 问题 | 严重度 | 来源 | 修复成本 |
|---|---|---|---|---|
| **甲** | Relay 三条路径返回 nil 结果，handler 新分支解引用 → **空指针 panic**（无渠道/分组超限/查询失败全部裸 500） | 高（回归） | 转发内核 | 一行级 |
| **乙** | 上游错误体未截断 → `RequestLog.Error varchar(1024)` 溢出 → **失败请求的日志整条丢弃** | 中（回归） | 服务层 | 一行 |
| **丙** | DataState「已有数据+刷新失败」分支吞掉默认插槽，**表格整块消失**（与组件注释承诺相反，五个页面受影响） | 高 | 前端 | 小 |
| 丁 | Gemini 入站转换静默丢弃 inlineData/fileData（**多模态丢数据**，无报错） | 高 | 转发内核 | 中 |
| 戊 | Anthropic/Responses 上游流干净 EOF 缺终止事件时，**截断被伪装成正常完成**（Gemini 版已修，两兄弟漏修） | 高 | 转发内核 | 小 |
| 己 | 非流式成功响应读体无上限（内存无界） | 中 | 服务层 | 小 |
| 庚 | ChannelIcon 默认图标内联写死亮色，暗色主题刺眼 | 中 | 前端 | 小 |
| 辛 | 详情抽屉/各 Modal 固定宽度，576-760px 视口溢出 | 中 | 前端 | 小 |

四个"高"里两个是本轮功能引入的回归（甲=真实状态码透传分支、乙=同一分支的另一缺陷），都是**小改动大收益**；丁/戊是静默数据损坏类（模型收不到图、截断当成功），与项目在 Gemini 侧、Anthropic 出站侧已经修过的同类问题一致——修一漏二的模式。

---

## 一、前端（UI / 布局 / 排版 / 可访问性）

### 高

**F-H1. DataState「已有数据 + 刷新失败」分支吞掉默认插槽——正在看的内容整块消失，与自身注释相反**
- 位置：`src/components/DataState.vue:46-79`
- 依据：模板是一条互斥条件链 `v-if(error&&hasData) → v-else-if ×3 → slot v-else`。alert 命中时 `<slot v-else>` 必然不渲染；而组件注释（10-12、45 行）承诺"已有数据后刷新失败：保留用户正在看的内容，只在顶部提示"。
- 触发面（五处 catch 均不清空数据、hasData 保持 true）：DashboardView（四张概览卡消失）、RequestLogPanel（日志表消失）、ChannelsView、KeysView、SettingsView。场景：已有数据 → 后端挂掉/15s 超时 → 点刷新 → 只剩一条 alert。
- 反证其为缺陷：GroupsView/ProxiesView 的 catch 主动清空 rows 走"整块错误面板"——六个页面行为分裂成两派，说明契约从未被满足。
- 修复：alert 分支退出互斥链（`<template v-if="error && hasData"><a-alert/><slot/></template>`），并去掉 GroupsView/ProxiesView 的手动清空。

### 中

**F-M1. ChannelIcon 默认图标配色内联写死亮色，暗色主题不适配**
- 位置：`src/components/ChannelIcon.vue:43-44`（`oklch(0.93 0.05 h)` / `oklch(0.45 0.13 h)` 内联样式，无法被 `[data-theme='dark']` 覆盖）
- 影响：渠道页名称列、看板渠道筛选下拉、日志「渠道」列、图标预览——所有未自定义图标的渠道。对照 GroupTag/ModelTag 都有暗色分支。
- 修复：亮度档位改 CSS 变量 + 色相经自定义属性传入，暗色换档（0.93→约0.32、0.45→约0.80）。

**F-M2. h() 渲染的 Modal 内容写死颜色，明暗主题均不达 AA**
- 位置：`ChannelsView.vue:405`（`color:#d46b08`，白底 3.55:1）、`KeysView.vue:182`（`color:#888`，12px 字 3.53:1）
- 修复：portal 元素仍继承 `:root` 变量，直接改 `var(--text-amber)` / `var(--color-text-secondary)`。

**F-M3. 详情抽屉固定 720px、各 Modal 固定宽，576-760px 视口溢出**
- 位置：`RequestLogPanel.vue:827`（width="720"，antd Drawer 无 max-width 保护）；同类：ChannelsView 760、KeysView 600、ModelPricingEditor 640、Groups/Proxies/批量粘贴 560、SettingsView 520
- 对照：`ChannelsView.vue:1109` 白名单抽屉已用 `min(760px, 94vw)`——已知此坑，其余漏改。全站统一 `min(NNNpx, 94vw)`。

**F-M4. 定价弹窗星期选择是 span+@click，键盘不可操作（WCAG 2.1.1 A 级失败）**
- 位置：`ModelPricingEditor.vue:234-243`。修复：改 `<button type="button" :aria-pressed>` + 焦点环（同文件删除按钮已是此写法）。

**F-M5. 看板工具栏窄屏溢出、中屏挤压**
- 位置：`PageToolbar.vue:16-21`（无 flex-wrap）+ `DashboardView.vue:468-505`（声明宽合计约 990px，媒体查询只覆盖卡片排）
- 修复：`.toolbar-panel` 加 `flex-wrap: wrap` + row-gap；或窄屏折叠筛选区。

### 低

| # | 位置 | 问题 |
|---|---|---|
| F-L1 | `MainLayout.vue:31-38/76-89` | 两个 onMounted 重复请求 /system/info（每次进页面两次）——主持者独立发现，删第一个即可 |
| F-L2 | `GroupsView.vue:393` vs `GroupTag.vue:84` | 暗色自定义色档位 62% vs 45% 不一致，"预览即最终效果"不成立；GroupTag 83 行注释（55%）与代码（45%）脱节 |
| F-L3 | 四个视图 | 时间格式三套口径（手工补零 vs toLocaleString 斜杠 vs YYYY/MM/DD）；空值占位 `-`/`—` 混用。建议提取 utils/fmtTime |
| F-L4 | `ProxiesView.vue:247/276-278` | 表格 middle 与全站 small 不一致；last_error 无截断撑高行 |
| F-L5 | `SettingsView.vue:329` | 7 列网格无响应式退档，窄屏标签换行卡高不齐 |
| F-L6 | `StatCard.vue:71-76` | --tone-bg 字面值不随暗色语义色提亮；fmt-toggle 暗色 hover 反馈偏弱 |
| F-L7 | `index.html:12` | theme-color 不随主题切换，暗色下地址栏仍米色 |
| F-L8 | `GroupsView.vue:281-287/361-368` | 取色控件 28×24 / 16×16 低于 WCAG 2.5.8 的 24×24，未纳入 pointer:coarse 44px 规则 |
| F-L9 | `ChannelsView.vue:589-722` | 渠道拖拽排序无键盘替代（WCAG 2.1.1）；后端已有整组提交接口，补上移/下移成本很低 |

---

## 二、后端服务层（api / store / model / pricing / config）

### 中

**B-中1. 上游错误体未截断 → 日志列溢出 → 失败日志整条丢弃**（本轮"真实状态码透传"引入的回归）
- 链路：`forward.go:263` 错误体上限 1MB → `convert/upstream.go:206`（及 Anthropic/Gemini/Responses 三个实现的兜底）JSON 解析失败时 `return string(body)` **无截断** → `relay_handler.go:211-217/246-254` 直接 writeError + finalizeLog → `entities.go:255` `Error varchar(1024)` → Postgres 22001 → `logwriter.go:89-95` 整条丢弃（含 usage/费用快照/报文留存）。
- 现实触发：网关 502 的 HTML 错误页（几 KB～几十 KB，JSON 解析必失败）。旧 502 路径经 `summarizeErrorBody`（300 字节）是安全的。
- 附带：超长 message（最长 1MB）也会回给客户端。
- 修复：api 层对 upMsg 按 rune 边界截断（如 800 字符）后再 writeError 与 finalizeLog；或 UpstreamErrorMessage 内部截断。一行修复双收益。

**B-中2. 非流式成功响应读体无上限**
- `forward.go:277` `io.ReadAll(resp.Body)` 无 LimitReader；请求体有 64MB 上限、响应体没有。批量 embeddings/异常放大响应可单请求数十～数百 MB（MaxConcurrency=64 只限并发数）。
- 修复：加可配置上限（如 2×MaxRequestBodyMB 或独立 max_response_body_mb），超限按错误处理。

### 低

| # | 位置 | 问题 |
|---|---|---|
| B-低1 | `admin.go:887-897` | bindChannelModel 更新分支不写 proxy_id（创建分支有），改了看似成功实则丢弃 |
| B-低2 | `admin.go:610-645` + `relay_handler.go:160-166` | resolveGroupWhitelist 在转发热路径每请求×每白名单条目一条 SELECT；keyCache 没缓存解析结果，失效点却都已具备——顺手补进 keyCacheEntry |
| B-低3 | `admin.go:1025-1038/1128-1141` | 分组重名回 500 而非 409（proxy 侧已有 isUniqueViolation 处理，不对称） |
| B-低4 | `admin.go:420-438/537-557` | create/updateChannel 不预检 group_id 存在性（FK 拒绝→500；proxy 侧有预检） |
| B-低5 | `admin_proxies.go:215-231` | updateProxy 两次 UPDATE 无事务：第二次失败留半截状态（unknown + 残留旧 error/latency） |
| B-低6 | `store.go:423-459` | proxy 引用无外键 + 删除/写入 TOCTOU；仅并发管理操作触发，本地单人概率极低 |
| B-低7 | `admin.go:1540-1544` | logDetail 吞掉报文查询错误：DB 故障返回 200+null 与"未留存"不可区分 |
| B-低8 | `admin.go:1423-1427` | /logs 无时间筛选时全表 COUNT(*)；建议 UI 默认带 range |
| B-低9 | `admin.go:165-172/222-234` | 白名单保存逐条 COUNT + 逐条 INSERT（几十条量级，可 IN + CreateInBatches） |
| B-低10 | `admin.go:1460-1506` | exportLogs 全量载入 + 字符串拼接（峰值 10-30MB，有 2 万行上界、低频，可接受） |
| B-低11 | `admin_backup.go:185-449` | importConfig 无总事务（设计取舍：可重复导入自愈 + warnings 如实汇报，知悉即可） |

### 服务层专项验证的正面结论
- /logs 与 /stats **无 N+1**：listLogs 两条固定查询；stats 系列单条聚合 SQL；listChannels 三条批量查询内存拼装。
- logwriter 队列满**丢弃不阻塞**、关闭防 panic、CloseAndFlush 语义正确。
- 本轮新改动的 api 消费链专项验证**全部通过**（ReleaseSlot 幂等、res.Candidate 非空保证、ErrGroupLimited 不冲突、finalizeLog 参数正确），唯一缺陷即 B-中1。

---

## 三、后端转发内核（relay / convert）

### 高

**R-高1. Relay 返回 nil 结果导致 handler 空指针 panic（内核契约缺陷，本轮回归）**
- 位置：`relay/service.go:266/286/294`（三条 `return nil, err`：候选查询失败 / attemptNo==0 撞分组限额 / 无可用渠道）× `api/relay_handler.go:211`（`if att := res.Attempt; ...`）
- 依据：err 非 nil 时 handler 第一件事就是对 res.Attempt 取字段——res 为 nil 必然 panic；`defer res.ReleaseSlot()` 因方法有 nil receiver 检查而幸存，掩盖了问题。gin.Recovery 把 panic 转成裸 500。
- 触发：请求未配渠道的模型名（配置初期极常见）、分组 RPM/TPM 打满、路由查询 DB 报错。精心设计的「无渠道时列出可用模型提示」（:294 的 availableModelsHint）与「分组超限回 429+Retry-After」全部不可达。
- 修复：Relay 契约改为「err 非 nil 时 res 也非 nil」（三条路径 `return res, err`）。一行级。

**R-高2. Gemini 入站请求转换静默丢弃 inlineData/fileData（多模态丢数据）**
- 位置：`convert/gemini.go:51-78`（parts 循环只有 text/functionCall/functionResponse 三个分支，无 default）
- 依据：带图/音频/PDF 的部件无声跳过。对照 Anthropic 入站处理 image、Responses 入站处理 input_image、Anthropic 出站对不支持内容显式报 ErrUnsupportedContent——唯 Gemini 入站既不转也不报。
- 后果：模型收不到图片按纯文本作答，无任何报错——静默数据损坏。
- 修复：inlineData 图片 → image_url 的 data URI；音频 → input_audio；fileData 走 ErrUnsupportedContent 显式拒绝。

**R-高3. Anthropic / Responses 上游流：干净 EOF 缺终止事件时，截断被伪装成正常完成**
- 位置：`convert/upstream_anthropic_stream.go:65-73`、`upstream_responses_stream.go:78-86`；对照**已修复**的 `upstream_gemini_stream.go:71-73`（有 stream_guard_test.go 锁定）
- 依据：Read 在 io.EOF 时无条件 finish()（合成 finish_reason:"stop" + usage + [DONE]），不检查是否见过 message_stop / response.completed；handler 只把非 EOF 当截断。Gemini 侧同类缺陷已修并注明修复前"客户端与计费都按成功处理"——两兄弟原样存在。
- 修复：照抄 Gemini 守卫；保留「零事件+EOF 合成空回复」语义（有测试锁定），有内容但无终止事件才判截断。

### 中

| # | 位置 | 问题 |
|---|---|---|
| R-中1 | `upstream_responses_stream.go:164-175` | response.failed 与 completed/incomplete 同路处理，error 字段丢弃 → 流中失败伪装成正常收尾，按 2xx 记账 |
| R-中2 | `convert/gemini.go:408-472/557-565`、`responses_stream.go:76-142/386-395` | 入站 Gemini/Responses 流式翻译器不读 finish_reason：length/content_filter 截断呈现为完整结束（Anthropic 侧已修，又是一家） |
| R-中3 | `convert/anthropic.go:21-46` | 入站 Anthropic 白名单丢弃 thinking（含 budget_tokens）/top_k/metadata：扩展思考被静默关闭，计费口径随之变化；响应侧 thinking 已能双向转换，唯请求侧断链 |
| R-中4 | `convert/responses.go:23-49` | 入站丢弃 previous_response_id：依赖服务端会话的 Responses 客户端上下文静默清空、答案"失忆"。短期检测到即回明确 400 |
| R-中5 | `upstream_gemini.go:197-205`、`upstream_responses.go:245-255` | 出站 Gemini/Responses 静默丢弃 input_audio/file/未知块（Anthropic 出站会显式报错）——ErrUnsupportedContent 机制只保护了一家 |
| R-中6·性能 | `relay/service.go:258-264` | 每次重试重复候选查询+策略查询（2 次 DB 往返×尝试数）；且轮询游标每次 attempt 前进一格，RR 分布被故障转移流量扭曲。循环外查一次、轮内内存过滤 |
| R-中7·性能 | `forward.go:175/181`、`upstream.go:89-104/130-138` | 每次尝试对同一报文做 2-4 次全量 JSON 解析+序列化（RewriteModel 的 map 往返还重排键序、大整数经 float64 失真）；大报文高重试场景 CPU/GC 热点 |
| R-中8·性能 | `relay/service.go:252/490-521` | defer recordFailures 的同步 DB 写挡在成功响应交付之前：撞了 3 个渠道后第 4 个成功的流式请求，首字节前多 3 次 UPDATE 往返。建议异步化（LogWriter 同思路） |

### 低

| # | 位置 | 问题 |
|---|---|---|
| R-低1·性能 | `upstream_anthropic_stream.go:60`、`upstream_responses_stream.go:73` | 每 Read 分配 16KB（Gemini 版已改 readBuf 复用，两兄弟漏改） |
| R-低2·性能 | `relay/proxy.go:229-271` | UsageTee 对每条 SSE data 行全量 JSON 解析只为找 usage；可先 bytes.Contains 粗筛 |
| R-低3 | `relay/service.go:401` vs `:409-419` | 失败请求 RetryCount 恒 0（仅成功路径设置）——故障时恰恰看不到转移规模 |
| R-低4 | `relay/forward.go:214-218` vs `:234` | Accept 头先复制后覆盖（死代码，注释与实际不符） |
| R-低5 | `limit.go:182/214-228`、`grouplimit.go:37-45` | ChannelState/GroupLimiter 的 map 无回收（RateLimiter 有 Sweeper，这两个没有） |
| R-低6 | `convert/gemini.go:62/74` | 工具调用 id 以函数名生成：同名并行调用 id 冲突 |
| R-低7 | `relay/service.go:259-267` | 重试间隙客户端断开：Trail 丢弃、日志误记"查询候选渠道失败: context canceled"；查库前先判 ctx.Err() |
| R-低8·备注 | `convert/anthropic.go:253-270` | tool_result 内图片块拍平丢弃（OpenAI tool 消息表达不了图片，半被迫，但与 Anthropic 出站"宁可报错"哲学不一致） |

### 新故障转移逻辑专项评估（本轮改动边界）
1. 共识豁免 × 熔断：**正确**——applyCooldown 覆盖的状态码集合与 requestShapeStatus 不相交，豁免不可能放过真渠道级冷却；豁免记录不进 NoteFailure，连击不受污染。✓
2. 短路路径名额：**正确**——Retryable 分支先 Release 再判物理短路，返回时名额已还、releaseSlot 为 nil、failures 未记。✓
3. defer recordFailures 时序：**逻辑正确**（所有 return 路径的收集都可见），但存在 R-中8 的性能问题。✓逻辑 / △性能
4. 客户端断开豁免 × 名额：**正确**（先释放再判 ctx.Err()）。附带 R-低7 的窗口瑕疵。

---

## 四、主持者独立复查与交叉验证记录

**独立复查**（未分配给子代理的范围）：
- `MainLayout.vue`：发现双 onMounted 重复请求 /system/info（= F-L1）。
- `theme.css` 令牌体系、`PageToolbar`/`DataState`/`AnimatedNumber`、`stats.go` 聚合查询、`listLogs`、`logwriter`、`RequestLog` 索引（idx_log_created 等）、`secure/crypto.go`（标准 AES-GCM）、`netguard`（测试齐全）：均健康。

**中档以上发现的交叉验证**（主持者逐行核对源码，全部属实，无一虚报）：
- F-H1（DataState 互斥链吞插槽）、F-M1（ChannelIcon 内联 oklch）、F-M2（#d46b08/#888）、F-M4（day-chip span+@click）
- B-中1（entities.go:255 `size:1024` + upstream.go:206 `return string(body)` 无截断）、B-中2（forward.go:277 `io.ReadAll` 无 LimitReader）、B-低5（updateProxy 双 UPDATE）
- R-高1（service.go:266/286/294 三处 `return nil, err` + handler:211 解引用）、R-高2（gemini.go:51-78 无 inlineData 分支无 default）、R-高3（upstream_anthropic_stream.go:65-73 EOF 无条件 finish()）

**特别说明**：R-高1 与 B-中1 均为本轮「按真实状态码回客户端」功能引入的回归——前者是 handler 新分支解引用了本可能为 nil 的 res，后者是新分支绕过了旧路径 300 字节截断保护。审查发现后已由主持者核实确认。

---

## 五、修复优先级建议与进度

> **修复进度**：
> - ✅ 批次 1（9eebf5c）：R-高1 nil panic、B-中1 错误体截断、F-H1 DataState 插槽
> - ✅ 批次 2/3（f166094）：R-高2 Gemini 多模态（图片/音频转换 + fileData/未知 MIME 显式拒绝）、R-高3 两处流截断守卫（含 R-低1 readBuf 复用）、R-中1 response.failed 错误分片、B-中2 响应体 128MB 上限、F-M1 ChannelIcon 暗色令牌、F-M3 九处弹窗/抽屉宽度统一 min(NNNpx, 94vw)
> - ✅ 转换语义补齐（968823a）：R-中2 finish_reason 透传、R-中3 thinking/top_k 私有字段往返、R-中4 previous_response_id 拒绝、R-中5 出站 default 分支 + input_audio→Gemini inlineData
> - ✅ 性能批次（本轮）：R-中6 候选查询移出重试循环（含 RR 游标语义修正）、R-中7 RewriteModel 顶层浅解析（RawMessage 透传，顺带修复 >2^53 整数失真）+ strip/requestIsStream/UsageTee 子串门控、R-中8 失败记账异步化、B-低2 分组白名单解析进 keyCache、B-低8 无时间筛选 COUNT 加保留期下界、B-低9 白名单保存批量化

1. ~~**立即（三个一行级/小改动，收益立现）**~~：✅ 已上线。
2. ~~**尽快（静默数据损坏）**~~：✅ 已上线（R-高2、R-高3）。
3. ~~**随后**~~：✅ 已上线（B-中2、F-M1、F-M3、R-中1）。
4. ~~**转换语义补齐批次**~~：✅ 已上线。
5. ~~**性能批次（按量级排期）**~~：✅ 已上线（R-中6/中7/中8、B-低2/低8/低9；F-M5 看板工具栏换行除外，归入前端低档批次）。
6. **择机**：其余低档一致性项（F-L 系列、F-M2/M4/M5、B-低1/3/4/5、R-低2~低7）。
