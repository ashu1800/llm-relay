# UI 规格书（实测自 llm.ohub.vip）

> 数据来源：Chrome DevTools Protocol 抓取登录后真实 DOM 的 `getComputedStyle`。
> 抓取脚本：`scripts/capture-layout.mjs`，原始数据：`docs/layout-dashboard.json`。
> 视口：1249 x 1277，页面标题「数据看板 - OHub」。

## 一、全局设计令牌（CSS 变量，实测原值）

| 令牌 | 值 |
|---|---|
| `--color-primary` | `#c87864` |
| `--color-secondary` / `--color-highlight` | `#afbeaf` |
| `--color-bg` | `#f8f5ee` |
| `--color-fg` | `#ffffff` |
| `--color-fg-shadow` | `0 0 .5rem 0 rgba(0,0,0,.1)` |
| `--color-text` | `#303030` |
| `--color-text-secondary` | `rgba(48,48,48,.65)` |
| `--color-border` | `#d9d9d9` |
| `--color-red` | `#ea4343` |
| `--color-orange` | `#f59e0b` |
| `--color-green` | `#10b37d` |
| `--color-blue` | `#06b6d4` |
| `--color-purple` | `#8b5cf5` |
| `--color-gray` | `#6b7280` |
| `--color-icon` | `#06b6d4` |
| `--color-icon-hover-bg` | `rgba(6,182,212,.12)` |
| `--scrollbar-thumb` | `#dcdcdc` |
| `--scrollbar-track` | `transparent` |
| `--scrollbar-width` | `.5rem` |
| `--ease-expo` | `cubic-bezier(.2, 1, .3, 1)` |
| `--font-family-base` | `Harding, STSong, SimSun, Songti SC, 宋体, Hiragino Sans GB, STHeiti, WenQuanYi Micro Hei, -apple-system, ..., serif`（衬线） |

## 二、布局骨架（实测尺寸，px）

```
main-layout            flex, bg #f8f5ee, 全屏
├── site-nav           header  高 64, padding 0 25, gap 16, bg #f8f5ee
│   ├── logo / brand
│   ├── nav-announcement   高 32, 圆角 999, 边框 1px #d9d9d9,
│   │                      padding 0 8, 文字 rgba(48,48,48,.65)
│   └── nav-actions    nav, flex, gap 8, 高 32（控制台 / 模型 / 文档 / 头像）
└── main-layout-body   position: relative
    └── console-layout flex
        ├── console-sidebar  aside 宽 192, padding 8, bg #f8f5ee, flex
        │   └── console-menu-list  nav, flex, gap 4
        │       └── console-menu-item  button 176x32, 圆角 999,
        │                              padding 0 8, gap 8, font-size 14.4
        └── console-content  section flex:1
            └── *-container  padding 8
```

**关键数字**：顶栏 64、侧边栏 192、菜单项高 32 圆角 999、内容区 padding 8。

## 三、卡片 / 面板（全站统一）

所有面板（`dashboard-toolbar`、`panel-block`、`summary-card`、`chart-card`、`heatmap-panel`、`detail-panel`）共用同一套规格：

| 属性 | 值 |
|---|---|
| background | `#ffffff` |
| border | `1px solid #d9d9d9` |
| border-radius | `8px` |
| box-shadow | `rgba(0,0,0,0.1) 0px 0px 8px 0px` |
| padding | `8px` |
| margin-bottom | `8px` |
| font-size / line-height | `14px` / `16.1px` |

## 四、栅格

全部使用 CSS Grid，**gap 恒为 8px**：

| 区块 | 规格 |
|---|---|
| `overview-row` | grid，左 `summary-grid` (501) + 右 `heatmap-panel` (501) |
| `summary-grid` | grid 2x2，每格 `summary-card` 246x68，flex，gap 16 |
| `chart-grid` | grid，`chart-card` 501x332（画布区 483x288） |
| `heatmap-body` | grid，gap `4px 8px` |

## 五、排版

| 元素 | 规格 |
|---|---|
| `section-title` (h2) | font-size **16px**，font-weight **700**，color **#c87864**，margin-bottom 8 |
| 面板正文 | font-size 14px，line-height 16.1px |
| body | font-size 16px，color #303030 |
| 菜单项 | font-size 14.4px，line-height 16.56px |

## 六、表格

| 元素 | 规格 |
|---|---|
| `ant-table-cell` (th) | 高 **39px**，背景 **#f8f5ee**（表头用页面底色而非白底） |
| 数据行 | 高 **39px** |
| 表格容器 | 圆角 8px 8px 0 0 |
| 分页 | `ant-pagination-mini`，高 24 |

## 七、控件

| 控件 | 规格 |
|---|---|
| `ant-picker` / `range-picker` | 高 **24px**（small 尺寸），圆角 6px，白底 |
| `ant-btn-primary` | 高 32，圆角 6px，bg **#c87864**，白字，padding `4px 15px` |
| 选中态胶囊 | bg `rgba(200,120,100,0.2)`，color `#c87864` |

## 八、数据看板信息架构（实测）

- **工具栏**：时间范围（今天 / 近3天 / 近7天 / 近30天）+ 刷新
- **概览四卡**：请求数量、消费金额、Token 量、成功率
- **请求热力图**：按日期 x 小时分布
- **图表区**：消耗分布、消耗趋势、模型调用分析、模型消耗占比
- **模型用量明细表**：模型 / 请求数 / 成功·失败 / 成功率 / Token（输入/输出/缓存）/ 消费金额

## 九、与本站的差异约定

1. 「消费金额」列**保留**，展示为**预估金额**（按录入单价折算），不做任何扣减。
2. 新增「模型定价」页：单价全部手工录入，支持固定倍率与**按时段倍率**
   （如工作日 9:00-12:00、14:00-18:00 双倍），时段按服务器本地时区判断。
   列表会提示「渠道白名单里有、但还没定价」的模型 —— 这类模型的费用恒为 0。
3. 剔除登录、用户、订单、工单、兑换、礼品、邮件、公告等模块。
4. 保留「请求日志」的详情抽屉与导出（接口与脚本仍在，界面按钮 2026-09-16 去掉）
   与计费过程还原。列表自身的筛选 2026-09-16 起不再使用，见第 9 条。
5. 新增站点专属鼠标光标（参考站用的是系统光标）：默认箭头与交互手型换成
   本项目自绘的图形，**浅色与深色各一套**，见
   `frontend/public/cursor-arrow-light.svg` / `cursor-hand-light.svg` /
   `cursor-arrow-dark.svg` / `cursor-hand-dark.svg`。两套的轮廓不同而不只是配色
   不同：浅色是长而窄的锐角轮廓（高宽比 1.47），深色是短而阔的圆角轮廓（1.19）。
   各自取本主题「主色实心块」的填充 + 描边（浅色 `#b15840` / `#ffffff`，
   深色 `#e8a48c` / `#3a241d`）。两套热区坐标必须逐字相同（箭头 `2 2`、
   手型 `9 2`）——同一点在不同主题下落到不同元素，用户只会以为是自己点歪了。
   输入框 I 型、拖拽、禁用、帮助四类刻意保留系统光标 —— 它们是功能性提示，
   自绘反而更难认。选择器清单来自对本站七个路由的 computed style 审计
   （不是照抄参考站），完整理由与踩坑记录见 `frontend/src/styles/theme.css`
   的「自定义光标」段。
   两套图形共用食指几何（x 7.4~10.8、指尖弧半径 1.7），所以热区相同这件事
   有几何依据，不只是注释里的承诺；审计脚本按主题分开跑，深色页面上出现浅色
   那套文件名会单独报「串主题」。
6. 请求日志的「首字耗时 / 总共耗时」两列合并为一列「任务耗时」：单元格里竖排
   两行（左侧一条**双色**竖条 + `首字` / `耗时`），数值按耗时分级着色
   （**首字** ≤10s 绿、10-30s 橙、>30s 红；**耗时** ≤20s 绿、20-60s 橙、>60s 红。
   两行各用一套阈值是因为首字才是「卡不卡」的那一下，总耗时把上游生成时间也算进去，
   长回答本来就要几十秒，用同一把尺子会把正常请求染红。
   判据与悬停说明见 `components/RequestLogPanel.vue` 的 `latencyClass` / `durTitle`）。
   合并的理由是这两个数回答的是同一个问题「这次调用等了多久」，分列时扫列表要在
   两处之间来回看，而两列合计 200px 换来的信息只有两个数；合并后列宽 120px。
   竖条按截图**断成两段**：上段跟首字、下段跟耗时，段色各自继承该行数值的
   `currentColor` —— 一整条单色只能表达「这次调用慢」，断成两段才看得出是
   首字慢还是生成慢。两段分占两行 grid 的两条轨道，因此与两行文字天然齐平；
   圆角只加在整条上（截图里两端圆角、中间交界处是直角）。
   秒值**一律补齐两位小数**（`8.70s` / `14.00s`）：位数一致时小数点在同一列上，
   扫一列数字不用重新找基准；不足 1 秒仍按毫秒显示，避免 2ms 被抹成「0.00s」。
7. 请求日志的列顺序按「读一行日志的顺序」重排：
   请求时间 → 模型 → 渠道 → 词元 → 任务耗时 → 费用 → 状态 → 密钥 → 操作。
   其中：
   - 「分组」列**移除** —— 同一行的模型、密钥两个胶囊本来就是按所属分组着色
     （`tagColorOf`），分组名仍在详情抽屉里；
   - 「渠道」列加上渠道图标（与渠道页、渠道下拉同一个 `ChannelIcon`），
     排障时要一眼认出「这条走的是哪条渠道」；
   - 「状态」「密钥」移到末尾：它们回答的是「结果如何」，夹在模型与渠道之间
     会打断前面那条「什么时候 → 哪个模型 → 哪条渠道 → 花了多少」的阅读线。
   列宽 2026-09-23 起改为**按内容动态测量**（见第 17 条）：下限仍是当年按实测收窄的
   那组值（第 10 条把「缓存命中」并入词元之后合计 1036，取「表头文字宽」与
   「内容最宽」的较大者 + 16px 内边距），运行时再按当前页内容逐列放宽。
   表格因此可能在窄窗口下宽于容器（此时横向滚动），固定列的遮挡有回归脚本盯着。
8. 列表（表格 / `.ant-list` / `.ant-tag`）里的**西文与数字**用参考站那份
   **Harding**，随包分发在 `frontend/public/fonts/Harding-Regular.ttf`；
   中文**不用**参考站的做法（它的中文是宋体），仍走 `--font-family-base`
   （Windows 上是微软雅黑）—— Harding 与后面几个系统衬线族都不含中文字形，
   中文会自动落到链尾，不需要额外拆元素。
   回退链 `'Harding', Cambria, 'Book Antiqua', 'Palatino Linotype', Georgia,
   'Times New Roman', var(--font-family-base)`：在参考站页面上同页量过字宽
   （14px，「日期 / 词元 / 耗时 / 模型名 / 百分比 / 金额」六串的平均偏差），
   Georgia 2.30 < Cambria 3.05 < Book Antiqua 3.12 < Palatino 5.47 < Times 7.45，
   但 Georgia 是旧式数字（3/4/5/7/9 下沉，一列数字会上下跳），
   Book Antiqua 又只有装了 Office 的机器才有（本机没有，此前整条链实际落在
   Georgia 上），所以按「字宽接近 + lining 数字 + 系统自带概率」排成上面这个顺序。
   这是商业授权字体：内部自用无碍，对外分发本项目前需自行确认授权。
9. 请求日志**并入数据看板**（2026-09-16），「请求日志」不再是一个页面：
   - 删掉热力图与四张图表（消耗趋势 / 消耗分布 / 模型调用分析 / 模型消耗占比）；
   - 四张概览卡从 2x2 改成**一排四张**（占满整行）；
   - 概览卡下面是请求日志列表（面板外壳 + 表格 + 分页；面板标题栏与导出按钮
     2026-09-16 已去掉，表体因此多出一整行），列与列宽见第 7 条、第 10 条与第 17 条（`scroll.x` 2026-09-23 起是各列宽之和，不再是固定值）；
   - 筛选栏只有一套，四个条件（时间范围 / 分组 / 渠道 / 模型）**只作用于概览卡**。
     时间范围改为看板原有的四档（今天 / 近3天 / 近7天 / 近30天），
     去掉了日志页原来的「近 1 小时」与「不限时间」；
   - **列表不吃这四个条件**（2026-09-16 站主要求「默认显示所有最新的请求」）：
     它恒定显示全部最新请求，只受两个排障深链约束 —— `?trace_id=…` 与
     `?status_class=error`（「仅失败」）。理由是列表的用处是「盯着最新发生了什么」，
     筛过之后反而看不到刚进来的调用，而刚进来的几条恰恰最该被看到。
     并进看板时两者本来共用一套条件，这次是**反向**约定（原来「必须共用」，
     现在「不许共用」），所以两侧都钉住了：组件不再声明那四个 props、
     看板不再传、查询串里也不许出现（`frontend/scripts/check-contracts.mjs`
     的「请求日志列表不吃筛选条件」区块，并配了变异测试确认它真的会红）。
     因为筛选框紧挨着列表，当初在工具栏右侧写了一句「筛选只作用于上方卡片」——
     不写的话界面就是在骗人。该标签已于 2026-09-17 **应站主要求移除**
     （提交 c56354b），当初「必须写这句话」的契约随之**反转成禁止式**：
     `check-contracts.mjs` 现在断言工具栏不得出现该文案。
     注意这是**产品决定**，不是遗漏 —— 列表不吃筛选的行为本身未变，
     仍由同一个契约区块（以及 `.shots/verify-list-nofilter.mjs`）钉住，
     所以别再按「界面没说明」把标签加回来；
   - 窗口由后端 `resolveRange()` 单点计算（`/stats/*` 用；`/logs` 不再带时间窗口）。
     原来列表也传 `range`，为的是与卡片落在同一个窗口里，那条理由随解耦一起消失；
   - 请求日志的四个筛选持久化键从 `logs-*` 改为 `dashboard-*`（合成一页即一套视角）；
   - 旧地址 `/console/logs` 保留为**带 query 的跳转**（`?trace_id=` / `?status_class=`
     这类深链原来就指着它）；
   - 后端的 `/stats/timeseries|models|channels|heatmap` 四个接口**保留**
     （有测试、有文档的 API 面），只是前端不再调用；`echarts` 依赖与
     `EChart.vue` / `utils/chartTheme.ts` 一并删除；
   - 代价：概览卡与筛选栏也占这 100vh 里的位置，表格可视行数从 17 行
     降到 14 行（1440x900 实测，第 15 行露出大部分）。表体高度当时从
     `calc(100vh - 201px)` 改为 `calc(100vh - 286px)`（去掉面板标题栏后又收窄
     40px，可视行数从 13 行回到 14 行；取 286 而不是实测的 284：那 2px 会让
     `.console-content` 多出隐形内滚）。这套固定预算在第 12 条里删掉了，逐项实测值见
     `frontend/src/components/RequestLogPanel.vue`。
     工具栏右侧那句提示不占额外高度（实测工具栏仍 50px、整页可滚 0px），
     这条账在 `.shots/verify-dash-layout.mjs` 里有回归。
10. 「词元」列改成**上下两排**、「缓存命中」列**移除**（2026-09-16）：
   - 表头 `词元（输入/输出/缓存）` → `词元`（括号里的说明挪到单元格悬停提示，
     顺带把这一列从 170px 收窄到 150 —— 原来撑宽列的是表头那串 154px 的括号）；
   - 单元格竖排两排、字号 12px：上排 `↑ 输入` `↓ 输出`，下排 `▣ 缓存词元` `命中率`；
     四个数放一格，是因为它们回答的是同一个问题「这次调用用了多少词元」，
     而原来两列合计 260px，扫一行要在两处之间来回看。
     箭头朝向按语义定（向上=送到模型、向下=模型返回），**与参考站截图里
     「↓ 在前、↑ 在后」的字形顺序刻意相反** —— 那个顺序读不出哪边是哪边；
   - 命中率的口径没变（`cached / (未命中 + 命中 + 缓存写入)`，与看板一致），
     只是从独立一列挪进词元格；`cached_tokens = 0` 的旧行因此显示成 `▣ 0 -`，
     与合并前两列的表现逐字一致，没有新增规则；
   - 箭头与缓存盒是**内联 SVG**（`stroke="currentColor"`，12px，线宽 1.5）：
     `↓` `↑` 这两个字符既不是西文也不是中文，会落到字体链尾的系统符号字体，
     线宽与大小随机器变（同第 8 条的字体问题）；图标颜色挂在「图标 + 数值」
     这一组上，图标与数字必然同色；
   - 配色**沿用项目现有语义色**（输入陶土 / 输出紫 / 缓存绿），
     没有照抄参考站那份（绿输入 / 紫输出 / 蓝缓存）：
     改色相等于改掉 theme.css 里已定档的语义（输入是花钱的那部分、缓存是省下来的）；
   - 行高不变（实测 40.25 / 41.25 两档）：两排 15px 用 -4px 上下负边距压成 22px，
     与「任务耗时」列同一手法 —— 行高一变，固定表体的可视行数与滚动位置都要跟着变；
   - 词元与任务耗时这两格的**居中**要用「块级 + `justify-content: center`」
     （渠道那格同理，一并改了）：全局规则是 `.ant-table-tbody > tr > td
     { text-align: center }`，而块级的 grid / flex 会撑满单元格、内容从左边起排
     （实测右空隙比左空隙大 42px / 27px / 14px）；但也不能改成 inline-block /
     inline-grid —— inline 级盒子要参与行盒，单元格那 14px 的行高会跟它叠起来，
     行高从 41px 涨到 49px。居中之后实测三格左右空隙相等，行高仍是 41.25；
   - 居中之外还要**把轨道宽度定死**（2026-09-16 修，站主反馈「绿色竖线不在
     一条直线上，强迫症受不了」）：`justify-content: center` 居中的是**轨道**，
     而 `auto` 轨道跟着内容长短伸缩 —— 任务耗时 45.7px（「-」）→ 87.05px
     （「466.50s」）、词元 64.81px → 149.53px，整块宽度逐行不同，居中所处的
     位置自然也不同。用 CDP 量 `::before`（绿竖条）的真实盒模型：改前竖条 x 有
     `[1033.02, 1036.63]` 两个位置、词元内容块左缘 5 个位置（漂 9.38px）；
     改成 `minmax(定值, auto)` 后两处都只剩**一个**位置（竖条 x `[1028.94]`）。
     定值按「列最窄时的可用宽度」倒推，不是按数值档位拍的：窗口窄到出现横向
     滚动时列回到声明宽度，任务耗时可用 104px、词元可用 134px —— 所以取
     「4px 竖条 + 6px 间隔 + 88px 文本轨道」与 128px，两者在任何窗口下都留有余量
     （词元第一版取 136px，在 1055 视口实测溢出 2px）。用 `minmax(_, auto)`
     而不是写死：真出现四位秒数或七位数词元时那一行让开，不会压到相邻单元格；
   - 列的声明总宽 1146 → 1036，1440 视口下容器 1182，剩余宽度由 antd 按比例
     分给各列（约 +13%），不手工调整与本次需求无关的列宽。
     （2026-09-23 起声明宽不再是固定值 —— 每列由脚本按当前页内容量出来，
     但「余量按比例分给各列」这条机制仍然生效：它是「声明宽 ≥ 内容宽」之外
     额外的一份宽裕，见第 17 条。）

11. **新日志扫光**（2026-09-16）：实时推送把一行新日志插到列表最上面时，
    沿这一行的**下分割线**从左向右扫过一道彩虹亮带，约 2 秒后从右端消失。
    - 触发面刻意很窄：**只有**实时推送带来的新行会闪。刷新页面、改筛选、
      翻页、点「刷新」都不闪 —— 那些操作整屏都在换，闪一排彩虹没有信息量；
      而「凭空多出来一行」不提示的话用户根本不会注意到列表变了。
      两条实时路径都覆盖：常态下新行直接前插（`onLive`），
      带排障深链时走安静重取（`load({silent:true})`，用重取前后的 id 差集认出新行）。
      （列表不再吃工具栏筛选之后，「有筛选」那条路只剩 trace / 仅失败两个深链。）
    - 亮带是表格外面那一层（`.log-table`）里的绝对定位元素，位置由 JS 量出来
      （`top/left/width` 都取自那一行的 `getBoundingClientRect`），所以它天生
      等于「整行宽 + 贴行底边」；那一层 `position: relative` + `overflow: hidden`，
      跑出容器的部分被裁掉，不会撑出滚动条，也不影响固定列的吸附
      （sticky 单元格最近的滚动祖先是 `.ant-table-body`，比它更近）。
      **为什么不画在 `tr` 的 `::after` 上**（第一版就是这么写的，1440 视口下看着
      一切正常）：`tr` 里一旦出现非单元格子元素（`::after` 就算一个），Chrome 在
      `table-layout: fixed` 下就不再把它多出来的宽度分给各列 —— 整张表从
      「铺满容器」塌回声明宽度，而表头是另一张表、照旧铺满，于是**右侧空出一条**，
      一直持续到动画结束。实测 1600 视口：每格 200/200/167/193/155/116/82/129/93 →
      155/155/130/…、行右边界 1575 → 1277（空 298px）。隔离验证过：去掉伪元素、
      只留 `position: relative`，列宽全程正常；只把 `position` 改成 `static`、
      留着伪元素，照样塌 —— 起因是那个伪元素。挂到单元格上也不行：固定列是
      `position: sticky`，它里面的绝对定位伪元素会落到行外（实测 `left:0` 的盒子
      跑成 x 1575→2909；改成右对齐又落在 148→1482，而行是 241→1575）。
      所以 `is-new` 这个类**不带任何样式**，只是「这一行正在做入场动画」的标记，
      `frontend/scripts/check-contracts.mjs` 会盯着它不被挂回 `tr`；
    - 动的是 **`background-position-x`**（`-50% → 150%`，带子占行宽 30%），
      不是元素的 `transform`。换成一个 translateX 的小元素跑出行外会撑大容器的
      可滚动溢出区；背景位移的盒子始终在行内（实测动画全程 `scrollWidth`
      === `clientWidth`，动画前后一致）；
    - `z-index: 3` 是为了盖住左右两个固定列（它们是 `position: sticky` +
      不透明底、`z-index: 2`）；
    - 缓动用 **`linear`**，不用项目里的 `--ease-expo`：那个曲线 0.3 秒就走完全程，
      剩下的时间停在右端不动，观感是「闪一下」。这里的时长本身就是需求；
    - 亮带高 3px（分割线是单元格的 1px 下边框，且算在行高内）：盖住它再往上
      压 2px，看起来才是一道光，而不是一条细线；
    - **彩虹是有意的用色例外**：项目其余部分严格走陶土主色系，这一处按站主要求
      用彩虹。色相取 antd 色板，两端 alpha 为 0（进出都是渐隐，不是硬边色块）；
    - 行的标记是「正在做入场动画的行 id 集合」，2.4 秒后清掉（2s 动画 + 余量）；
      不给行数据加 UI 字段 —— 那些字段会跟着进详情、进导出；
      首屏列表落地之前一律不标记（`loadedOnce`）：实测刷新页面的 4.5 秒窗口里，
      恰好入库的一条真实请求会因「rows 还是空的、无从比对」被当成新增，
      表现为「一刷新就闪」，与这条约定不符；
    - 「减弱动态效果」不用在这里重复处理：`theme.css` 末尾那条全局规则
      （`prefers-reduced-motion: reduce` 下把所有 `animation-duration` 压到
      `0.01ms`）会让它变成瞬时，终态在 150%（本来就不可见），功能不受影响。

11b. **入场动效改为三档可选**（2026-09-17 站主提）：第 11 条那道彩虹扫光之外，
    再加两档，在「系统设置 → 界面动效」里选，选中即刻生效、不用刷新。
    - 三档：`sweep` 彩虹扫光（**默认**，即第 11 条，行为一个字节没改）、
      `slide` 自上滑入（新行的各单元格 `translateY(-8px)` + 淡入，0.45s）、
      `glow` 光晕脉动（整行泛起一层主色底 + 行底一道光带，1.6s 呼吸两下）。
      刻意没有「完全关闭」：系统层面的 `prefers-reduced-motion` 才是统一静音路径，
      再多一个只对一块区域生效的开关只会让人以为"关了但还在动"。
    - 偏好存在浏览器本地（`localStorage` 的 `llm-relay-log-fx`），与「运行参数」无关、
      不进配置备份 —— 它是**这台机器这个浏览器**的观感偏好，不是服务端行为。
      读不到 / 存了不认识的值一律回落默认档（容错理由见 `utils/effects.ts`）。
    - 档位只有一个真源（`utils/effects.ts` 的 `LOG_FX_OPTIONS`），
      设置页、面板的 `data-fx`、效果层的 CSS 选择器、设置页的迷你预览四处都对它，
      `check-contracts.mjs` 钉住四处集合完全相等：id 写错不会报错，只会"选了没反应"。
    - 实体在 `components/NewLogEffect.vue`，三档共用同一个位置数据（面板量好的
      `top/left/width`），换档不重取数据、不重挂表格，只换这一层怎么画。
    - `slide` / `glow` 那两档动的是**单元格**的 `transform` 与 `background-image`。
      它们安全的原因与扫光的坑不冲突：那一次的问题是「`tr` 里多了一个非单元格子元素」，
      改变了 `table-layout: fixed` 的列宽分配；而 `transform` 与 `background-image`
      都是绘制期属性、不参与布局，也不会改变固定列（`position: sticky`）的吸附基准。
      这条结论不是推理出来的，是 `verify-log-effects.mjs` 的 layout 模式实测的：
      三档下**表头高 / 表体高 / 首行各格宽 / 行右边界 / 表头右边界 / 行高 / 页面横溢出 /
      固定列位置**全部逐项相等。
    - **踩过的坑（写法级）**：`slide` / `glow` 的选择器必须整条包进 `:global(...)`，
      不能写成 `:global(.log-table[data-fx='slide']) :deep(tr.is-new > td)` ——
      scoped 编译器对这种组合会**丢掉后半截**，实测编译产物只剩
      `.log-table[data-fx="slide"]`，规则落到 `tr` 上，而 `transform` 挂在 `tr` 上
      基本看不出效果：整档动画静默失效、不报任何错（回归脚本先发现了它）。
      `check-contracts.mjs` 现在禁止那种写法（判定前先剥注释，
      免得注释里的反例自己把自己判违规）。
    - 回归脚本：`.shots/verify-log-effects.mjs`（`layout|sweep|slide|glow|reduced|all`），
      自造探针行（`trace_id` 前缀 `logfx-probe`，跑完删掉）走真实推送链路。

12. 看板「左侧栏与右侧容器底边对齐」（2026-09-17 站主反馈「主页左侧侧边栏和右侧的
    组件容器底部显示不一致，不在一条水平线上」）：
    - 现象：右侧日志面板的底边比左侧栏最后一行高 7.6px，且与视口高度无关
      （768 / 800 / 900 / 977 / 1050 / 1200 实测都是这 7.6px）；
    - 根因：面板自己那 8px 下外边距。表体高度原来是 `calc(100vh - 286px)`，
      那笔预算把「面板下边距 8」也算了进去，可它落在面板底边**之下** ——
      面板底边于是停在视口 - 16px 处（内容区下内边距 8 + 面板下边距 8），
      而左侧栏的底部留白只有 8px。加上卡片排高度会随文案换行在 83~98px 之间变
      （实测），这笔账本来就是近似值，差在哪一项上完全看不出来；
    - 做法：删掉那笔固定预算，改成**弹性分配** —— 路由 `meta.fill` 让内容区变成
      高度确定的纵向容器（`MainLayout` 的 `.content-inner.is-fill`），
      再看板 `.dashboard`、日志面板、antd 那几层 div、表体逐层
      `flex: 1 1 auto; min-height: 0` 把剩余高度传下去，表体拿到的就是
      「面板里除表头与分页之外剩下的高度」（链条与逐条理由见
      `RequestLogPanel.vue` 的 `.log-table` 那一段）。
      面板自己的 8px 下外边距同时去掉；表体的 `scroll.y` 改为 `'100%'` ——
      它现在只负责让 antd 渲染「表头/表体分离」的固定表头结构，不再承担尺寸计算。
      卡片换行变高、工具栏多一句提示、顶上多出一条默认密钥告警，表体都自己让位，
      底边始终与左侧栏齐平；
    - 为什么只给看板加 `meta.fill`：其余页面按内容自然增高（系统设置比视口高，
      要能整页滚），给它们加 `height: 100%` 会让滚到底时少 8px 留白 ——
      下内边距落在固定高度的盒子里，不再计入可滚范围（实测 scrollHeight 少 8px）；
    - 回归：`.shots/verify-bottom-align.mjs`（各视口两侧底边差 0、无隐形滚动条、
      表头固定、固定列吸附、筛空后表体仍占满、其它路由几何与截图不变）。

13. 看板「词元数量卡片」的紧凑/完整双格式（2026-09-17 站主提出）：
    - 两种格式是彻底的两套口径，不是「大了才缩写」：
      完整 = 整数千分位（`1,234,567`）；
      紧凑 = 一律带单位（站主明确要求百万以下也用 M）：`>=1e9` 用 B、其余用 M，
      固定两位小数、不足补 0（`123,456 → 0.12M`、`1,234,567 → 1.23M`、
      `1.1e9 → 1.10B`）。固定小数位 + `tabular-nums` 让宽度恒定，
      推送补间与格式切换都不会横向抖动；
    - 格式实现为 `AnimatedNumber` 的第四个预设 `compact`（与 int/money/percent
      同级）：格式属于「全站要一致」的那类口径，散在各页面迟早对不上，
      以后别的卡片要接同一格式直接 `format="compact"`；
    - 切换入口：卡片右侧 `StatCard` 新增的 `suffix` 插槽里放一个 32px 图标按钮
      （`SwapOutlined` + tooltip + aria-pressed）。尺寸刻意小于左侧 48px 图标块
      ——主视觉与辅助控件要有层级；圆角 8px 与图标块一致；hover 浅底取卡片
      根上 tone 变量的 `--tone-bg/--tone-ink`（词元卡 blue 调），跟卡片同一色彩
      体系而不是另一套灰。`suffix` 插槽用 `useSlots` 判空才渲染容器：
      `margin-left:auto` 的弹性项哪怕空着也会占布局位，其余三张卡片不能受影响；
    - 切换动画：`Transition out-in` 包住 `AnimatedNumber`，key 只绑格式 ——
      数值推送不触发过渡，滚动补间仍归 AnimatedNumber 的 rAF；
      点按钮时旧值 0.2s 淡出上移 4px、新值淡入下移进入。
      `prefers-reduced-motion` 由 theme.css 全站块把 transition 压到 0.01ms，
      自动降级为直接换文本，无需本组件单独处理；
    - 选择持久化：`persistedChoice`（键 `dashboard-token-fmt`，合法值
      `full|compact`，默认 `full`）—— 与看板筛选同一机制，阅读习惯跟着人走；
      脏值回落 `full`，隐私模式下退化成「不记住」。


14. 第二轮审查的低档细节打磨批次（2026-09-17，代码审查报告-第二轮.md）：
    - 时间格式收拢为单一出口 `utils/fmtTime`（YYYY-MM-DD HH:mm:ss，空值 `—`）：
      原来四份实现各写一套（日志页补零、密钥/设置页 toLocaleString 的斜杠长相、
      代理页 YYYY/MM/DD），同一份数据在不同页面是不同长相；
    - `ChannelIcon` 默认图标的 oklch 亮度档位改为全局令牌 `--ch-icon-bg-l/ink-l`
      （色相仍由内联 `--ch-h` 随渠道名散列传入），暗色主题换暗底亮字 ——
      内联写死 oklch(0.93…) 的样式 [data-theme='dark'] 永远覆盖不到；
    - 全站弹窗/抽屉宽度统一 `min(NNNpx, 94vw)` 写法（九处），576-760px 视口
      不再溢出（antd 的 Drawer 没有 max-width 保护）；
    - `h()` 渲染进 portal 的内容也用颜色令牌（`var(--text-amber)` 等），
      不再写死 #d46b08/#888 —— portal 元素仍继承 :root 变量；
    - 定价弹窗的星期胶囊从 span+@click 改为原生 button + aria-pressed +
      focus-visible 焦点环（WCAG 2.1.1 A 级：键盘用户此前配不了时段规则）；
    - 工具栏允许换行（`flex-wrap` + row-gap）：看板工具栏声明宽合计近千像素，
      窄屏不换行会溢出面板；
    - `--tone-bg` 一律 `color-mix` 从语义色派生（不照抄浅色 rgba 字面值），
      暗色主题的提亮版语义色得以传导到卡片色调底与格式切换按钮的 hover；
    - `theme-color` meta 跟随主题切换（暗色 #202020），浏览器地址栏不再
      在深色界面外围留一圈米色；
    - 分组表单预览胶囊与 GroupTag 的暗色混白比例统一为 45%（原 62%，
      同一自定义色在预览与列表里颜色不同，「预览即最终效果」不成立）；
    - 代理页表格改 `size="small"` 与全站一致，上游错误两行截断 + title 悬停全文；
    - 系统设置数据概览网格补 900px/600px 两档退列（7→4→2）；
    - 取色控件与色板在 `pointer: coarse` 下用透明伪元素把命中区扩到 44px
      （视觉尺寸不变 —— 直接加大尺寸会让色块本身变大）。

15. 看板工具栏在矮窗口下被压扁（2026-09-17 站主反馈「浏览器窗口高度不高时，
    就会出现纵向居中没有正常居中，而是贴到了底部边缘的问题」）：
    - 现象：窗口高度一矮，工具栏里的控件不是纵向居中，而是贴着面板下边框；
      窗口越矮贴得越紧，但**常规视口（977 高）下完全正常** ——
      这一条只在矮窗口复现，所以看静态截图、跑常规尺寸的回归都发现不了；
    - 根因：工具栏（`PageToolbar.vue` 的 `.toolbar-panel`）是看板 `.dashboard`
      这个纵向弹性容器的子项，而看板整页锁高（路由 `meta.fill` →
      `MainLayout` 的 `.content-inner.is-fill`，见第 12 条）。弹性子项默认
      `flex-shrink: 1`，父容器高度不够时它就被按比例压缩；而它自己的
      `min-height` 只有 42px —— 比内容实际需要的 50px（32px 控件 +
      上下各 8px 内边距 + 上下各 1px 边框）**还小**，于是内容比盒子高，
      只能朝下溢出，看起来就是「贴到了底部边缘」。
      弹性容器的子项一被压缩，`align-items: center` 就再也居不了中：
      居中需要盒子比内容高，而这时盒子比内容矮；
    - 实测（CDP 量内容盒上下留白，下留白为负即溢出）：
      视口高 1000 → 面板 46.53px、下留白 −3.47；
      900 → 43.16px、−6.84；850 起触到 `min-height` 42px、一直 −8；
      1100px 宽那档工具栏换行成 3 行（自然高 88px），最差到 −46。
      即「越矮压得越扁」，与站主描述一致。
      修好后各档恒为 50px（单行）/ 88px（1100px 宽换行档），上下留白全为 0/0；
    - 做法：`.toolbar-panel` 加 `flex: 0 0 auto` —— 工具栏永远按内容占高，
      不再参与弹性压缩；剩余高度的预算交给下面的日志面板（它有 `min-height: 0`，
      本来就负责吃满剩余空间，见第 12 条那条弹性链）。
      **没有**改 `min-height`：42px 是空内容时的下限（对齐参考站
      `dashboard-toolbar` 的高度基准），它比内容矮正是这个 bug 的另一半原因，
      把兜底寄托在它身上只会把失效点推到更矮的窗口；
      也**没有**写死 `height`：窄屏换行后自然高会变（实测 1100px 宽 = 88px），
      写死高度等于换个方向再犯一遍；
    - 为什么值得单列一条：这个坑「只在矮窗口出现」+「修法是一行看起来像冗余的
      声明」，两件事叠起来使它极易被顺手删掉 —— 删掉不报错、不失败，
      只是窗口一矮就回到贴底边。所以 `check-contracts.mjs` 钉住
      `.toolbar-panel` 里必须有 `flex: 0 0 auto`（或 `flex-shrink: 0`）
      且不许出现写死的 `height`；
    - 回归：`.shots/verify-toolbar-center.mjs`（两档宽度 × 视口高 1000→240，
      逐档断言上下留白非负、单行上下对称、内容不溢出、无隐形滚动条、
      工具栏高度与视口高无关，并确认日志面板仍吃满剩余高度）。
      修复前 75 项失败，修复后全部通过。
    - **验收必须在容器（8888）上做，不能只看 dev（5173）**
      （2026-09-17 这次真踩了）：改完先只在 5173 上验证，交了「已修复」，
      站主回「问题并没有得到修复」—— 因为他开的是 8888，而那是**旧构建**。
      前端产物被 `deploy/Dockerfile` 烘进 Go 二进制
      （stage 1 构建 dist → stage 2 `COPY --from=frontend-build /src/dist`），
      所以「源码改了」≠「8888 上生效了」，必须重建镜像 + 重建 app 容器。
      判定这件事只读源码没用（源码确实改了），要读**浏览器里实际生效的样式**：
      `.shots/probe-toolbar-css.mjs` 把 `document.styleSheets` 里命中
      `.toolbar-panel` 的规则原文 + computed style + 几何一起打出来。
      这次它就是决定性证据：5173 报 `flex: 0 0 auto`、面板 50px；
      8888 报 `flex: 0 1 auto`、面板 42px（同一个矮窗口 1400x700）。
      部署走项目自带的一键脚本 `deploy/install.sh`（含源码同步 → 构建 →
      8899 端口临时库预检 → `up -d --no-deps app` 灰度切换 → 健康检查），
      不要手工拼 docker 命令：它会在切换前先决定「新镜像起不起得来」，
      预检不过就保持线上旧版本，另外它会保留 `deploy/.env` 与数据库卷。
      部署前先给旧镜像打回滚标签；实测本次中断 874ms、数据库容器未被重启。

16. 新行入场动画与卡片数字**同时开始**（2026-09-18 站主反馈「新日志入场动画结束后
    统计卡片里的数值才会变动，正常应该是一起变动的，而不是先播放入场动画后
    再进行数值变动」）：
    - 现象：新行插进来、彩虹扫光开始跑，卡片上的数字纹丝不动；
      要等动画快跑完（有时跑完之后）数字才开始滚。**不是每次都这样** ——
      两件事的间隔在 0～2 秒之间漂移，所以看起来时好时坏；
    - 根因不在前端，也不在数字补间（`AnimatedNumber` 收到新值就立刻起补间），
      而在服务端两条推送循环的**相位**：`liveLogsLoop` 每秒查一次 id 增量、
      `liveStatsLoop` 每两秒算一次今日汇总，是两个独立的 `time.Ticker`，
      起点互不相干。同一条请求入库造成的「新行插进来」与「卡片数字变了」
      因此被拆成两拍；
    - 实测证据（新增 `.shots/verify-live-sync.mjs`，打的是线上产物 8888）：
      插入一行探针日志后，新行 487ms 就出现在表格里（`is-new` 标记 + 亮带，
      动画开始），卡片数字到 1551ms 才动 —— 空窗 **1064ms**；
      修复后两者落在同一拍（差值即一次聚合查询的时间，毫秒级）。
      顺带证伪了一个直觉猜测：这与查询快慢无关，今天那两条聚合查询走
      `idx_log_created` 索引，实测 0.096ms；
    - 做法：日志循环查到新行、广播 `logs` 帧之后，往 `statsKick`
      （容量 1 的非阻塞通道）投一个信号，统计循环的 `select` 多一个 `case`，
      收到就立刻算一次并推送。两帧落在同一条 WebSocket 上，顺序仍是
      「行先到、数字随后」，间隔只剩一次聚合查询的时间，入场动画与数字滚动
      因此同时开始；
    - **没有**把统计也改成 1 秒一拍：那只是把窗口从 2s 缩到 1s，两拍仍然是两拍，
      平均还差半秒；而且空转的聚合查询翻倍 —— 它们绝大多数时候算出来一模一样
      （没变化就不推，白算）。定时器保留为兜底节拍（清理历史数据、手工改库 +
      kick 被合并掉的那些时刻），所以「2 秒」这个说法仍然成立，只是不再是
      新日志的更新延迟；
    - 回归：`.shots/verify-live-sync.mjs`（在页面里每 50ms 采一次
      「卡片四个数字 + `.is-new` 行数 + 亮带数」的时间线，断言数字首次变化
      与新行首次出现同帧、且互不抢先）。修复前该项失败（差 1064ms），
      修复后通过。
      脚本默认跑 **3 个回合**而不是 1 个：这个 bug 的根因是相位差，相位差在
      0～2 秒之间漂移，单回合很可能碰巧通过 —— 修复前后都是。实测三回合的
      delta 是 54 / 36 / 43ms（判据 ≤100ms，即两个采样间隔）。
      另有两条**正向**证据，防止「同时到达」被「干脆不动」骗过去：
      「动画还在放的期间数字就已经变过」（站主那句话的字面判据）与
      「数字是滚上去的（采样里存在既非旧值也非新值的中间值）」；
      最后按词元数把卡片终值与 `/stats/summary` 核对，确认新值真的吃进了这一行。

    - **二次反馈（同日）**：站主指出问题仍然存在 —— 看板当前不是「今天 + 全站」
      视角时（选了近 3 天 / 近 7 天 / 近 30 天，或筛了任意分组 / 渠道 / 模型；
      筛选会被持久化，所以这是常用路径），推送合并不了，前端走的是
      `setTimeout(1500)` 的静默重取：数字的变动被人为推迟 1.5 秒 + 一次请求耗时，
      而动画 sweep 2s / glow 1.6s 早已播完 —— 上面的服务端修复对这条路径
      天然无效，回归脚本又只测了默认视角，所以测不到。
      日志列表的深链筛选（`?trace_id=` / `?status_class=`）那份同款 1500ms
      是同一个问题（重取回来才标新行、才做动画）。
    - 做法：抽 `useLive.createThrottledLiveReloader`（首帧**立即**执行 +
      1 秒最小间隔 + 在途合并），看板卡片与日志深链两处都换掉固定延迟。
      保留最小间隔而非完全裸奔：持续流量下服务端大约每秒推一帧，无节流就是
      每秒好几次聚合请求；1000ms 下最坏情况数字也只比动画晚一拍请求耗时
      （几十毫秒），仍在动画播放中，观感上就是「一起变」。
      「今天 + 全站」直接合并的快路径不变（它是零请求的最优路径）。
    - 回归：新增 `.shots/verify-live-sync-filtered.mjs` —— 先把时间范围切到
      「近 7 天」，再插探针行采样。判据放宽到 ≤400ms（比默认视角多一次
      HTTP 请求的余量），正向证据与词元核对沿用（核对口径用 `range=7d`）。
      教训记一笔：**「修好了」的结论只在被测过的视角上成立** ——
      看板有两条更新路径（直接合并 / 静默重取），回归必须两条都覆盖。

17. **请求日志每列的宽度按内容动态调整**（2026-09-23 站主要求「每列根据显示内容
    动态调整列宽，保证列内容至少要显示完整」）：
    - 改之前只有「模型」列是动态的（2026-09-20 起），其余九列是一组人工实测的
      定宽常量 —— 数据一换长度就截断：模型名都短时多出大半空白，换一批更长的
      渠道名 / 密钥名又重新出现省略号。
    - 机制（`RequestLogPanel.vue` 的 `COL_BOUNDS` / `CONTENT_MEASURE` /
      `remeasureColumns`）：`rows` 变化后（`nextTick`）逐列量当前页内容的宽度，
      取「该列下限」与「内容所需宽度 + 16px 内边距 + 6px 呼吸」的较大者，
      clamp 到该列的 `[下限, 上限]` 作为 `:width` 的声明值；`scroll.x` 取各列宽
      之和（`columnsTotal`）—— 模板里每列的 `:width` 与这个求和共用同一份定义。
    - 下限就是原来那组定宽常量（它同时是「表头文字 + 内边距」的下界，
      所以列再窄也挤不到表头）；上限是**软上限**，超出部分仍走 `ellipsis` +
      悬停 `title` —— 一个 60 字符的内部模型代号不值得把整行撑到看不见别的列。
    - **为什么声明宽度就够**：表格是 `table-layout: fixed`，声明宽就是列宽的下界；
      而声明总宽小于容器宽时 antd 还会把余量按比例分给各列（第 10 条实测），
      于是「声明宽 ≥ 内容宽」等价于「内容不被截断」—— 这正是要守的那条不变量。
    - **量什么有硬性要求**：必须是**不被列宽压缩**的元素，`scrollWidth` 才等于
      「装下全部内容需要的宽度」（普通块级元素上它等于 `clientWidth`，量不到内容）。
      又因为不能把内容块改成 `inline-block` / `inline-flex`（第 10 条实测：inline 级
      原子盒会把单元格行高从 41px 顶到 49px），块级 flex / grid 的列一律量**内部已
      `nowrap` 的子元素**，再把固定前缀加回去：渠道 = 名字 + 18 图标 + 6 间距、
      任务耗时 = 文本 + 3 竖条 + 6 间距。费用格为此包了一层普通 inline 的
      `.txt-cell` 作为锚点（inline 不影响行高，且它的 rect 宽度就是文本宽度）。
    - 阈值与联动：列宽变化 < 2px 不写回（1px 的抖动不值得让整张表重排，而且列宽
      一抖、所有飞行中的扫光位置都要跟着重算）；写入后补一次 `repositionBeams()`；
      列表字体（Harding）就绪后重量一次 —— 字体没加载时量到的是回退字体的宽度。
    - 代价与边界：**内容比容器宽时表格会横向滚动**，这是「保证内容完整」的必然
      结果（实测 1440 视口 30px、1280 视口 190px、1600 视口一屏装下），
      滚到最右端时右侧固定列不遮挡内容。行高、扫光、表体高度预算都不受影响
      （三档动效下的列宽逐项一致，`.shots/verify-log-effects.mjs` 有回归）。
    - **锚点必须只命中一列**（2026-09-23 补）：测量是全局查询（`.ant-table-tbody`
      拼上锚点选择器），跨列命中就会把别列的内容算进本列。密钥列原来用
      `.group-tag`，而模型名与密钥名**都用 GroupTag 渲染** —— 模型列那枚更宽的胶囊
      （`deepseek-v4.1-flash` ≈ 123px）被算进了密钥列，密钥列因此常年 145px，
      而那一页内容只要 71px（站主反馈「密钥列为什么占用了那么宽的列宽」）。
      修法是密钥列改用只属于它的 `.key-tag`（`<GroupTag class="key-tag">`，组件把
      class 透传到根元素），实测修前 `.group-tag` 命中第 1、8 列（模型、密钥），
      修后 `.key-tag` 只命中第 8 列；密钥列 145 → 101px（下限），表格总宽 1241 → 1207。
      这类错误静态检查看不见（跨列命中要渲染出来才知道），所以判据放在
      `scripts/check-log-columns.mjs`：逐个锚点统计它落在哪几列，多于一个就失败；
      锚点列表也从 `CONTENT_MEASURE` 读，不在脚本里另抄一份（抄的那份会在换锚点后
      继续盯着旧名字，看着在跑其实一列都没命中）。
    - 回归：`scripts/check-log-columns.mjs`（3 视口 × 3 种每页条数，逐格断言
      「内容没有被截断」「单元格没有横向溢出」「滚到最右固定列不遮挡」）、
      `scripts/check-table-widths.mjs`（运行时表的列宽必须全部联动，不许混进
      裸数字 —— 它此前只认数字字面量，这张表改成表达式后被**静默跳过**）、

18. **列宽：下限也得跟着自适应走**（2026-09-24 站主反馈「渠道列宽明显不对」）：
    - 现象：渠道列占 137px，而那一页渠道名只要 47px（`D1余额`），
      **44px 全是空白**；同期其它列只空 28-34px。站主一眼看出来。
    - 根因不是「列太窄」而是**反方向**：`channel: [130, 240]` 的下限 130 是
      2026-09-16 那组人工定宽常量的遗留，2026-09-23 推广「按内容量」时被原样
      留下 —— 机制是 `max(下限, 内容所需)`，**下限一比内容高，自适应就对那一列
      永远失效**。改成按真实数据里最长的渠道名定：
      `CodingPlan` 82px（离屏实测文字宽）+ 24（图标 18 + 间距 6，
      `CONTENT_MEASURE.channel.pad`）+ 16（单元格内边距）+ 6（呼吸）= **128**。
      修后各视口列宽 128-151px，空白与其它列齐平（22-35px）。
    - 踩过的两个坑，都值得记住：
      1. **下限与测量必须用同一套加法**。第一版算成 122，漏了 pad 里的 6px 间距，
        于是 1280 视口下 `CodingPlan` 稳定地差 7px 被省略号截掉。
      2. **`scrollWidth ≤ clientWidth` 对省略号列不敏感**。被
         `text-overflow: ellipsis` 截断时 `scrollWidth` 会被压到接近
         `clientWidth`（实测 clientW=74 / scrollW=75，而真实文字宽 82）——
         列宽越不合适、名字越被省略、这条判据反而越「绿」。所以
         `check-log-columns.mjs` 补了「离屏 nowrap span 量文字真实宽度」的判据，
         量的是**单元格**可用宽（减左右内边距）而不是锚点自己 ——
         费用列的锚点 `.txt-cell` 是 `display:inline`，`clientWidth` 恒为 0，
         拿它比会得出「需 73px 只有 0px」这种假失败。
    - 顺带修好的一处：费用列此前在 8888（改动前）实测溢出了 2px
      （`¥0.000080` 装不进 90px），列宽自适应生效后不再溢出。
    - **哪些判据试过但不能用**（记下来免得下次重蹈）：把「列宽不该比内容宽太多」
      做成断言时，四种写法全都不成立 —— 运行时绝对阈值 24px 把全部列误判
      （正常列也空 30+px，因为 antd 按比例分配容器余量）；放宽到 40px 就放过了
      真问题（旧代码渠道列空 38px 仍 PASS）；「比其它列中位数高 X px」实测只差
      8px；静态「下限 vs 表头+内边距」把 model(76)/tokens(96)/speed(74) 一并误判
      （那些余量是内容真的需要）。根因是「合理余量」取决于该列真实数据 ——
      静态看不出、运行时又因余量分配浮动。**最终只做报告不判定**：
      `check-contracts.mjs` 打出各列「下限与硬下界之差」，
      `check-log-columns.mjs` 打出各列实测空白量，由人一眼核对。
    - 仍待处理（本次未动，已实测确认是既有问题）：速度列的「流」胶囊左缘有
      3 个取值（`1069.5 / 1063.9 / 1068.1`，1440 视口），旧代码同样如此 ——
      站主 2026-09-23 反馈过一次，当时判据是「只允许 1 个取值」，现在回归成 3 个。
      与本次列宽改动无关（改动前后取值集合一致）。
      `frontend/scripts/check-contracts.mjs`（测量锚点与模板对得上、每列都绑了
      `colW`、`scroll.x` 由 `columnsTotal` 给出）。

18. **三处对齐修正**（2026-09-23 站主反馈）：
    - **收起态的侧栏图标不居中**：`.console-menu-item` 的左右 10px 内边距是给
      展开态的文字留的，而收起态（56px 宽）里只剩一枚 15px 的图标 —— 实测
      左留白 18px、右留白 23px，整列图标比侧栏中轴偏左 5px，与上面居中的 LR 圆标
      （12/12）、底部那枚主题按钮（20.5/20.5）不在一条竖线上。
      修法是收起态改 `justify-content: center` + 去掉内边距，四处元素的中心线
      统一落到 28px（即 56/2）。展开态的左对齐是**有意**的（图标要贴文字那一列），
      所以规则只写在 `.is-collapsed` 下，展开态实测未变（仍是 18px）。
      回归：`.shots/measure-sidebar.mjs` —— 逐元素打印左/右留白与中心线，
      窄视口下侧栏默认就是收起的，脚本先看状态再决定点不点那颗按钮。
    - **速度列的「流」胶囊不在一条纵线上**：速度是「输出词元 ÷ 耗时」，位数随请求变
      （73 / 162 / 2800），而这一块在单元格里是居中的 —— 整块宽度逐行不同，
      居中所处的位置自然也不同，左侧那枚胶囊就跟着左右跳。这与第 10 条「任务耗时」
      是同一个成因，处理方式也照它：给数值轨道一个最小宽度
      （`.spd { min-width: 66px }`），常见位数下整块宽度恒定。
      66px 按「列最窄时的可用宽度」倒推：速度列下限 118px 减左右内边距 16px 得
      102px，再减去胶囊（12px 字 + 左右各 8px 内边距 + 1px 边框 ≈ 30px）与间距 6px；
      库里实测最大 7000 tok/s（p99 是 268），四位数放得下。
      回归：`scripts/check-log-columns.mjs` 断言「所有胶囊左缘只允许一个取值」
      （数值那一项只统计流式行 —— 非流式行没有胶囊、自己居中是设计如此）。
    - **表格里的标签偏左 4px**（站主反馈「200 的状态码看着轻微向左偏移」）：
      antd 给 `.ant-tag` 默认加了 `margin-inline-end: 8px`，那是给「多个标签并排」
      留的间距，而表格里每个格只有一个标签、靠 `td` 的 `text-align: center` 居中
      —— 那 8px 会被算进行盒宽度，胶囊整体偏左 4px 不说，**居中的判定本身就错了**
      （实测状态码格：左留白 0.8px、右留白 8.8px）。
      修法是 `.ant-table-tbody > tr > td .ant-tag { margin-inline-end: 0 }`：只写进
      `td` 里 —— 工具栏那些可关闭的筛选标签（DashboardView 的 trace / 状态）是并排的，
      间距要留着（实测表外仍是 8px）。
      受这条规则影响的是全站表格里所有单标签格：渠道页的协议/状态/启用、分组页的
      策略/默认/启用、密钥页的启用、代理页的协议/状态/启用 —— 一次全修掉。
      回归：`.shots/check-table-tags.mjs` 逐页量「标签到单元格内容区左右缘」两段留白
      （左右相等才算过），并核对每一页的实际落点 —— 原来那份页面列表里有
      `/console/pricing`，而路由表**没有**这一条，它一直落到 catch-all 再重定向到看板，
      于是「量的是看板的数据」却报告成「模型定价」。`scripts/measure-tables.mjs`
      有同一个问题，一并修正并加了落点核对。

19. **侧栏底部两枚文字按钮被截断成「收…」「退…」**（2026-09-25 站主截图反馈）：
    - **根因是横向空间不够，不是文字太长**。展开态侧栏 192px，减左右各 8px 内边距，
      页脚可用宽只有 176px；而那一行是三项：两枚 `flex: 1` 的文字按钮 + 一枚 32px
      灯泡 + 2×4px 间距 → 每枚文字按钮只剩 **68px**。
      一枚「收起侧栏」的完整内容宽是 101px（左内边距 10 + 图标 15 + 间距 8 +
      文字 58 + 右内边距 10），68px 装不下，`text-overflow: ellipsis` 于是把文字
      压成「收…」。渲染态实测：label `scrollWidth 58 > clientWidth 25`，两枚按钮
      都一样（`.shots/repro-sidebar-footer.mjs` 会把页脚每个盒子的宽度摊开）。
    - **两枚并排无论如何都放不下**：101×2 + 32 + 8 = 242 > 176。所以修法不是缩字号
      或砍文字，而是**把页脚改成竖排**（`flex-direction: column`）：两枚文字按钮
      各占满整栏（`width: 100%`，176px），文字不再参与宽度争抢；灯泡
      `align-self: flex-end` 贴右缘，右缘与两枚胶囊齐平。
    - 一个容易踩的坑：竖排下**不能**沿用 `flex: 1` —— 列方向的主轴是纵向，
      `flex: 1` 会拉伸的是**高度**（按钮会被撑高成一整块）。两枚按钮写
      `flex: 0 0 auto` 保持 32px 行高。
    - 收起态本来就是竖排三枚图标（第 18 条钉着那三处的中心线），修完展开/收起
      成了同一套排布，只是展开态文字可见。实测三枚图标中心线仍统一在 28px（=56/2）。
    - 回归：`.shots/verify-sidebar-footer.mjs` —— 展开态断言两枚标签
      `scrollWidth ≤ clientWidth`、标签宽度 ≥ 文字**实际排版宽度**（用 Range 量，
      不是拿 scrollWidth 自证）、两枚等宽且左缘对齐、按钮占满页脚内容宽；
      收起态断言三枚图标中心线都落在中轴。点击行为另由
      `.shots/verify-sidebar-theme-btn.mjs` 用 CDP 真实鼠标事件钉住（灯泡换了
      `align-self` 之后，命中区不能被邻居盖住，也不能只剩 `el.click()` 能过）。
